<div class="login__wrapper" style="background-image: url('<%= image_path('Sign-up.png') %>')">
  <div class="container ctn-login">
    <div class="login-top">
      <h3>Crea tu asignatura</h3>
    </div>
    <div class="goal-new__box">
      <%= simple_form_for(@goal) do |f| %>
        <% if @goal.errors.any? %>
          <div class="errors-container">
            <ul>
              <% @goal.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <%= f.input :name, placeholder: "De qué se trata?" %>
        <%#= f.input :subject, placeholder: "What subject it belongs to?"%>
        <%= f.association :subject, collection: Subject.all, promt: "A qué area pertenece?" %>
        <%= f.input :difficulty, collection: ["easy", "medium", "hard"]%>
        <%= f.input :description, placeholder: "Add a description to the goal" %>

        <div class="text-center">
          <%= f.submit "Done", class: "btn btn-homepage xs-margin" %>
        </div>
        <!-- [...] all the fields -->
      <% end %>

    </div>
  </div>
</div>
